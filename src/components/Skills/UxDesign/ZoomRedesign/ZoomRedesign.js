import React from 'react';

import cntClasses from '../../../Layout/ProjectContent/ProjContent.module.css'

const ZoomRedesign = () => {
    return (
        <div className={cntClasses.cntContainer}>
            <div className={cntClasses.cntWrap}>
                <h2>Zoom Redesign</h2>
                <div>Improve Zoom for online course conditions</div>
            </div>
        </div>
    )
};

export default ZoomRedesign;
